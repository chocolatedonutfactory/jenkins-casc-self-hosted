# Jenkins Configuration
JENKINS_IMAGE_VERSION=2.550-jdk25
JENKINS_HTTP_PORT=8080
JENKINS_AGENT_PORT=50000

# Keycloak Configuration
KEYCLOAK_IMAGE_VERSION=24.0
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_DB_VENDOR=h2
KC_REALM_NAME=jenkins-lab

# Nginx Configuration
NGINX_IMAGE_VERSION=1.27-alpine
NGINX_JENKINS_PORT=8443
NGINX_KEYCLOAK_PORT=8444

# Environment Configuration
LAB_DOMAIN=localhost

# Shared Secrets (Change these in production!)
OIC_CLIENT_SECRET=jenkins-secret
